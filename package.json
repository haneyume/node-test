{
  "name": "node-test",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "electron-test",
    "vue-test",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "dev:electron": "cd electron-test && yarn dev",
    "dev:vue": "cd vue-test && yarn dev"
  }
}
